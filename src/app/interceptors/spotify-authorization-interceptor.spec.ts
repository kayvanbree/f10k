import { SpotifyAuthorizationInterceptor } from './spotify-authorization-interceptor';

describe('SpotifyAuthorizationInterceptor', () => {
  it('should create an instance', () => {
    expect(new SpotifyAuthorizationInterceptor()).toBeTruthy();
  });
});
